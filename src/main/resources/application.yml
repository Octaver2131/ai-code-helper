spring:
  application:
    name: ai-code-helper
  profiles:
    active: local
server:
  port: 8080
  servlet:
    context-path: /api
langchain4j:
  community:
    dashscope:
      chat-model:
        model-name: qwen-max
        api-key: <your api key>
      embedding-model:
        model-name: text-embedding-v4
        api-key: sk-<your api key>
      streaming-chat-model:
        model-name: qwen-max
        api-key: sk-<your api key>
bigmodel:
  api-key: <your api key>
